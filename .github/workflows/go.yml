name: Go CI
image: gitlab/dind
on:
  push:
    branches: [ master ]
  pull_request:

jobs:

  build:
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: github.com/ontology-layer-2
      GH_ACCESS_TOKEN: ${{ secrets.ROLLUP_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v2
        with:
            persist-credentials: false
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - run: sudo curl -L -N "https://github.com/gakonst/foundry/releases/download/nightly-d08a59ec16e07329c70f9a5ccf5dbb92baf367f3/foundry_nightly_linux_amd64.tar.gz" | sudo tar zxv -C /bin
      - run: npm install --global yarn && yarn
      - run: yarn build
      - run: ls
      - name: Inject insteadOf configuration for l2 private repo
        run: |
          git config --global url."https://${{ secrets.ROLLUP_ACCESS_TOKEN }}:x-oauth-basic@github.com/ontology-layer-2".insteadOf "https://github.com/ontology-layer-2"
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.15

      - run: ls
      - run: docker pull maxxing/riscv32-toolchain:latest
      - name: Go Fmt & Test
        run: bash ./.github/workflows/.gha.precheck.sh